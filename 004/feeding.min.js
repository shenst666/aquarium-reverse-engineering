(function () {
  'use strict';
  const SPECIES = [
    // -------- FRESHWATER --------
    { id: 'betta', name: 'Betta (Siamese fighting fish)', category: 'freshwater', diet: 'carnivore',
      adultMode: { times: 2, perFeed: '3-4 pellets', total: '6-8 pellets' },
      juvenileMode: { times: 3, perFeed: '2-3 pellets', total: '6-9 pellets' },
      foods: ['High-protein betta pellets', 'Frozen bloodworms (2x/week)', 'Frozen brine shrimp', 'Live blackworms (treat)'],
      avoid: ['Generic tropical flakes', 'Dried bloodworms (cause bloat)'],
      fastDay: true,
      notes: 'Bettas are surface feeders with small stomachs. One day of fasting per week prevents constipation, a leading cause of swim-bladder issues.'
    },
    { id: 'guppy', name: 'Guppy', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Pinch of flakes', total: 'What they eat in 30 sec' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Small frequent feeds' },
      foods: ['Quality tropical flakes', 'Crushed pellets', 'Frozen daphnia', 'Spirulina flakes (1x/week)'],
      avoid: ['Large pellets (mouth too small)'],
      fastDay: false,
      notes: 'Continuous feeders that benefit from small, varied meals. Include vegetable matter (spirulina) to prevent constipation in fancy strains.'
    },
    { id: 'molly', name: 'Molly', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Generous pinch', total: '30-second rule' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Small frequent feeds' },
      foods: ['Vegetable flakes', 'Spirulina pellets', 'Blanched spinach/courgette', 'Frozen daphnia'],
      avoid: ['Pure protein diets (cause bloat)'],
      fastDay: false,
      notes: 'Mollies are heavy on vegetable matter — at least 60% of diet should be plant-based. Algae grazing is natural and beneficial.'
    },
    { id: 'platy', name: 'Platy', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Pinch of flakes', total: '30-second rule' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Small frequent feeds' },
      foods: ['Tropical community flakes', 'Spirulina flakes', 'Frozen brine shrimp', 'Blanched vegetables'],
      avoid: [],
      fastDay: false,
      notes: 'Hardy and undemanding. Vary diet between protein and vegetable matter for best colour development.'
    },
    { id: 'neon-tetra', name: 'Neon Tetra', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Small pinch', total: '20-30 sec consumption' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Tiny meals' },
      foods: ['Crushed tropical flakes', 'Micro-pellets', 'Frozen daphnia', 'Frozen cyclops'],
      avoid: ['Large pellets', 'Bloodworms in excess'],
      fastDay: false,
      notes: 'Mid-water feeders with small mouths. Food should sink slowly through the water column; sinking pellets get ignored.'
    },
    { id: 'cardinal-tetra', name: 'Cardinal Tetra', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Small pinch', total: '20-30 sec consumption' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Tiny meals' },
      foods: ['Crushed tropical flakes', 'Micro-pellets', 'Frozen baby brine shrimp', 'Frozen daphnia'],
      avoid: ['Large food items'],
      fastDay: false,
      notes: 'Slightly more carnivorous than neons. Live or frozen baby brine shrimp brings out the best red colouration.'
    },
    { id: 'cory', name: 'Corydoras Catfish', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 1, perFeed: '1-2 sinking wafers', total: 'Evening feed' },
      juvenileMode: { times: 2, perFeed: 'Crushed wafers', total: 'Small frequent meals' },
      foods: ['Sinking catfish pellets', 'Algae wafers', 'Frozen bloodworms', 'Live blackworms (treat)'],
      avoid: ['Surface-only foods (they\'ll starve)'],
      fastDay: true,
      notes: 'Bottom feeders that need food reaching the substrate. Feed after lights-out so other fish don\'t steal their share. Not just "cleanup crew" — they need dedicated meals.'
    },
    { id: 'plec', name: 'Bristlenose Plec', category: 'freshwater', diet: 'herbivore',
      adultMode: { times: 1, perFeed: '1-2 algae wafers', total: 'Evening feed' },
      juvenileMode: { times: 2, perFeed: 'Half wafer', total: 'Twice daily' },
      foods: ['Algae wafers', 'Blanched courgette', 'Blanched cucumber', 'Driftwood (essential for digestion)'],
      avoid: ['Heavy protein diet (causes liver damage)'],
      fastDay: false,
      notes: '85% of diet should be plant matter. Provide a piece of driftwood permanently — bristlenoses need to rasp wood for digestion.'
    },
    { id: 'angelfish', name: 'Angelfish', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: '5-7 pellets', total: '10-14 pellets' },
      juvenileMode: { times: 3, perFeed: '3-4 pellets', total: '9-12 pellets' },
      foods: ['Cichlid pellets', 'Frozen bloodworms', 'Frozen brine shrimp', 'High-quality flakes'],
      avoid: ['Tubifex worms (disease risk)'],
      fastDay: true,
      notes: 'Adults are slightly carnivorous. Vary diet to maintain pair-bonding and breeding readiness. Stop feeding 24h before water changes.'
    },
    { id: 'discus', name: 'Discus', category: 'freshwater', diet: 'carnivore',
      adultMode: { times: 3, perFeed: 'Beef-heart cube or equivalent', total: 'Spread through day' },
      juvenileMode: { times: 5, perFeed: 'Small portion', total: 'Continuous through day' },
      foods: ['Beef-heart mix (homemade)', 'Frozen bloodworms', 'Discus-specific pellets', 'Live blackworms'],
      avoid: ['Mammal meat in excess (digestion issues)', 'Tubifex (disease)'],
      fastDay: true,
      notes: 'Heavy feeders that demand water-change rigour: 30-50% changes daily for juveniles, 2-3x weekly for adults. Mistreatment shows in stunted growth.'
    },
    { id: 'goldfish-fancy', name: 'Goldfish (Fancy varieties)', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: '2 minutes worth', total: 'No leftover food' },
      juvenileMode: { times: 3, perFeed: '90 seconds worth', total: 'No leftovers' },
      foods: ['Sinking goldfish pellets', 'Blanched peas (deshelled)', 'Gel food', 'Spirulina'],
      avoid: ['Floating pellets (gulp air, swim-bladder issues)', 'Bread'],
      fastDay: true,
      notes: 'Sinking pellets only — fancy goldfish swallow air with floating food and develop chronic swim-bladder problems. Weekly pea day prevents constipation.'
    },
    { id: 'goldfish-common', name: 'Goldfish (Common/Comet)', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: '2-3 minutes worth', total: 'Generous feed' },
      juvenileMode: { times: 3, perFeed: '90 seconds worth', total: 'Smaller meals' },
      foods: ['Goldfish pellets', 'Blanched lettuce', 'Live daphnia', 'Earthworms (chopped)'],
      avoid: ['Tropical fish food (wrong nutrient balance)'],
      fastDay: true,
      notes: 'These grow into pond fish (30+ cm). Feed accordingly — and please consider a pond setup when they outgrow the indoor tank.'
    },
    { id: 'oscar', name: 'Oscar', category: 'freshwater', diet: 'carnivore',
      adultMode: { times: 1, perFeed: '4-6 large pellets or equivalent', total: 'Once daily, large meal' },
      juvenileMode: { times: 2, perFeed: '3-4 pellets', total: 'Two meals' },
      foods: ['Large cichlid pellets', 'Frozen krill', 'Frozen prawns (chopped)', 'Earthworms'],
      avoid: ['Live feeder fish (disease + nutritionally poor)', 'Mammal meat'],
      fastDay: true,
      notes: 'Stop feeding live feeder goldfish — they introduce disease and the thiaminase damages oscar liver. Quality pellet base + varied frozen.'
    },
    { id: 'apisto', name: 'Apistogramma (Dwarf Cichlid)', category: 'freshwater', diet: 'carnivore',
      adultMode: { times: 2, perFeed: 'Small portion', total: '60-second rule' },
      juvenileMode: { times: 3, perFeed: 'Tiny portion', total: 'Spread through day' },
      foods: ['Frozen baby brine shrimp', 'Frozen bloodworms', 'Sinking micro-pellets', 'Live grindal worms'],
      avoid: ['Large flake food'],
      fastDay: false,
      notes: 'Strongly prefer live or frozen food. Pellets accepted but enthusiasm matters — well-fed apistos colour up dramatically.'
    },
    { id: 'rasbora', name: 'Rasbora (Harlequin/Chili)', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Small pinch', total: '20-30 sec consumption' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Tiny meals' },
      foods: ['Crushed flakes', 'Micro-pellets', 'Frozen daphnia', 'Frozen cyclops'],
      avoid: ['Large pellets'],
      fastDay: false,
      notes: 'Tiny mouths require small particles. Slow-sinking food works best — they feed in the upper to mid water column.'
    },
    { id: 'danio', name: 'Zebra Danio', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Pinch of flakes', total: '30-second rule' },
      juvenileMode: { times: 3, perFeed: 'Crushed flakes', total: 'Smaller meals' },
      foods: ['Tropical flakes', 'Micro-pellets', 'Frozen daphnia', 'Frozen brine shrimp'],
      avoid: [],
      fastDay: false,
      notes: 'Voracious surface feeders. Will outcompete slower tankmates — feed in two locations to ensure others get fed.'
    },
    { id: 'gourami', name: 'Gourami (Honey/Dwarf/Pearl)', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 2, perFeed: '4-6 pellets', total: '8-12 pellets' },
      juvenileMode: { times: 3, perFeed: '3-4 pellets', total: '9-12 pellets' },
      foods: ['Tropical flakes', 'Micro/medium pellets', 'Frozen bloodworms', 'Spirulina flakes'],
      avoid: [],
      fastDay: false,
      notes: 'Anabantoids — they breathe surface air. Floating pellets work well. Vary protein and vegetable matter for best colour.'
    },
    { id: 'killifish', name: 'Killifish', category: 'freshwater', diet: 'carnivore',
      adultMode: { times: 2, perFeed: 'Small portion', total: '60-second rule' },
      juvenileMode: { times: 3, perFeed: 'Tiny portion', total: 'Spread through day' },
      foods: ['Live blackworms', 'Frozen daphnia', 'Frozen baby brine shrimp', 'Carnivore micro-pellets'],
      avoid: ['Pure flake diet'],
      fastDay: false,
      notes: 'Strong preference for live or frozen food. Many species refuse dry food entirely — research your specific killifish species before purchase.'
    },
    { id: 'shrimp', name: 'Cherry/Crystal Shrimp', category: 'freshwater', diet: 'omnivore',
      adultMode: { times: 1, perFeed: 'Tiny pinch (every 2-3 days)', total: 'Less is more' },
      juvenileMode: { times: 1, perFeed: 'Tiny pinch (every 2-3 days)', total: 'Mostly biofilm' },
      foods: ['Shrimp-specific pellets', 'Blanched spinach (1x/week)', 'Indian almond leaves', 'Mulberry leaves'],
      avoid: ['Copper-containing fish food', 'Anything labeled with copper sulphate'],
      fastDay: true,
      notes: 'Most shrimp deaths are from overfeeding, not underfeeding. They graze biofilm constantly — supplemental food only every 2-3 days.'
    },
    { id: 'snail', name: 'Mystery/Nerite Snail', category: 'freshwater', diet: 'herbivore',
      adultMode: { times: 1, perFeed: 'Algae wafer (every 2-3 days)', total: 'Mostly grazes algae' },
      juvenileMode: { times: 1, perFeed: 'Algae wafer (every 2-3 days)', total: 'Mostly biofilm' },
      foods: ['Algae wafers', 'Blanched courgette', 'Blanched spinach', 'Calcium supplement (cuttlebone)'],
      avoid: ['Copper-containing food'],
      fastDay: true,
      notes: 'Snails graze constantly on biofilm and algae. Supplement with vegetables only if tank is too clean. Calcium is essential for shell health.'
    },
    // -------- MARINE --------
    { id: 'clownfish', name: 'Clownfish', category: 'marine', diet: 'omnivore',
      adultMode: { times: 2, perFeed: '5-7 pellets', total: '10-14 pellets' },
      juvenileMode: { times: 3, perFeed: '3-4 pellets', total: '9-12 pellets' },
      foods: ['Marine pellets', 'Frozen mysis shrimp', 'Frozen brine shrimp (gut-loaded)', 'Marine flakes'],
      avoid: ['Freshwater-formulated food'],
      fastDay: false,
      notes: 'Hardy and adaptable. Mysis shrimp is the staple — soak briefly in vitamin supplement (Selcon, VitaChem) once per week.'
    },
    { id: 'tang', name: 'Yellow/Blue Tang', category: 'marine', diet: 'herbivore',
      adultMode: { times: 2, perFeed: 'Sheet of nori + pellet', total: 'Continuous grazing' },
      juvenileMode: { times: 3, perFeed: 'Smaller portions', total: 'Continuous grazing' },
      foods: ['Nori sheets (clipped)', 'Marine algae flakes', 'Spirulina pellets', 'Mysis shrimp (1x/week)'],
      avoid: ['Pure carnivore diet (causes lateral line erosion)'],
      fastDay: false,
      notes: 'Tangs need to graze almost continuously. A nori clip should be present in the tank most of the day. Lateral line erosion is the #1 sign of dietary failure.'
    },
    { id: 'goby', name: 'Watchman/Yellow Goby', category: 'marine', diet: 'carnivore',
      adultMode: { times: 2, perFeed: 'Small portion', total: '60-second rule' },
      juvenileMode: { times: 3, perFeed: 'Tiny portion', total: 'Spread through day' },
      foods: ['Frozen mysis', 'Frozen brine shrimp', 'Marine pellets (small)', 'Live copepods (treat)'],
      avoid: [],
      fastDay: false,
      notes: 'Bottom feeders with small mouths. Sinking food works best. Pair with pistol shrimp for natural symbiosis and entertainment.'
    },
    { id: 'wrasse', name: 'Sixline/Fairy Wrasse', category: 'marine', diet: 'carnivore',
      adultMode: { times: 2, perFeed: '5-7 pellets', total: '10-14 pellets' },
      juvenileMode: { times: 3, perFeed: '3-4 pellets', total: '9-12 pellets' },
      foods: ['Marine pellets', 'Frozen mysis', 'Frozen Hikari brine', 'Live pods'],
      avoid: [],
      fastDay: false,
      notes: 'Active swimmers with high metabolism. Need 2-3 feedings daily. Hide and sleep early so feed before lights-out.'
    },
    { id: 'cardinalfish', name: 'Banggai/Pyjama Cardinalfish', category: 'marine', diet: 'carnivore',
      adultMode: { times: 2, perFeed: 'Small portion', total: '60-second rule' },
      juvenileMode: { times: 3, perFeed: 'Smaller portion', total: 'Spread through day' },
      foods: ['Frozen mysis', 'Frozen brine shrimp', 'Small marine pellets', 'Live brine'],
      avoid: ['Pure flake diet'],
      fastDay: false,
      notes: 'Slow, deliberate feeders. May be outcompeted by faster tankmates — target-feed with a turkey baster if needed.'
    },
    { id: 'anthias', name: 'Lyretail Anthias', category: 'marine', diet: 'carnivore',
      adultMode: { times: 4, perFeed: 'Small portion', total: 'Frequent small meals' },
      juvenileMode: { times: 5, perFeed: 'Tiny portions', total: 'Almost continuous' },
      foods: ['Frozen mysis', 'Frozen calanus', 'Frozen cyclops', 'Live copepods'],
      avoid: ['Once-daily feeding (will starve)'],
      fastDay: false,
      notes: 'Demand 3-4 feedings per day minimum — adapted to constant zooplankton grazing on reefs. Auto-feeders strongly recommended.'
    },
    { id: 'damsel', name: 'Damselfish', category: 'marine', diet: 'omnivore',
      adultMode: { times: 2, perFeed: 'Small pinch', total: '30-second rule' },
      juvenileMode: { times: 3, perFeed: 'Smaller pinch', total: 'Smaller meals' },
      foods: ['Marine flakes', 'Marine pellets', 'Frozen mysis', 'Spirulina flakes'],
      avoid: [],
      fastDay: false,
      notes: 'Hardy and aggressive eaters. Will dominate feeding time — ensure shyer tankmates also get fed.'
    },
    { id: 'blenny', name: 'Lawnmower/Tailspot Blenny', category: 'marine', diet: 'herbivore',
      adultMode: { times: 2, perFeed: 'Small portion + nori', total: 'Plus continuous grazing' },
      juvenileMode: { times: 3, perFeed: 'Smaller portions', total: 'Plus continuous grazing' },
      foods: ['Nori sheets', 'Marine algae flakes', 'Spirulina pellets', 'Algae from rocks'],
      avoid: ['Pure protein diet'],
      fastDay: false,
      notes: 'Algae grazers — only thrive in tanks with established algae growth. New tanks lacking algae require daily nori supplementation.'
    }
  ];
  const $ = (sel, root = document) => root.querySelector(sel);
  function init() {
    const calc = $('#feeding-calculator');
    if (!calc) return;
    populateSpeciesSelect();
    bindCalculate();
    bindReset();
    bindExports();
    bindSpeciesChange();
    bindI18nVoice();
  }
  function bindI18nVoice() {
    if (!window.FishhearI18N) return;
    const ui = window.FishhearI18N.buildControlRow('fc-i18n-voice-mount', () => {
      // No re-render needed for labels currently shown in English-only fields (species/food names),
      // but the spoken sentence below will use the newly selected language.
    });
    if (!ui) return;
    ui.speakBtn.addEventListener('click', () => {
      if (!lastResult) { flashError('Calculate a feeding plan first.'); return; }
      window.FishhearI18N.speak(buildSpokenFeedingSentence(lastResult));
    });
  }
  function buildSpokenFeedingSentence(r) {
    const I = window.FishhearI18N;
    if (!I) return '';
    return `${I.t('feeding_plan_for')} ${r.species.name}: ${r.mode.times} ${I.t('times_per_day')}, ${r.mode.perFeed} ${I.t('per_feeding')}. ${I.t('daily_total')}: ${r.mode.total}.`;
  }
  function populateSpeciesSelect() {
    const select = $('#fc-species');
    if (!select) return;
    const fw = SPECIES.filter(s => s.category === 'freshwater').sort((a, b) => a.name.localeCompare(b.name));
    const mw = SPECIES.filter(s => s.category === 'marine').sort((a, b) => a.name.localeCompare(b.name));
    const fwGroup = document.createElement('optgroup');
    fwGroup.label = 'Freshwater';
    fw.forEach(s => {
      const opt = document.createElement('option');
      opt.value = s.id;
      opt.textContent = s.name;
      fwGroup.appendChild(opt);
    });
    const mwGroup = document.createElement('optgroup');
    mwGroup.label = 'Marine';
    mw.forEach(s => {
      const opt = document.createElement('option');
      opt.value = s.id;
      opt.textContent = s.name;
      mwGroup.appendChild(opt);
    });
    select.appendChild(fwGroup);
    select.appendChild(mwGroup);
  }
  function bindSpeciesChange() {
    $('#fc-species').addEventListener('change', () => {
      const id = $('#fc-species').value;
      const species = SPECIES.find(s => s.id === id);
      const hint = $('#fc-species-hint');
      if (species && hint) {
        hint.textContent = `${species.diet.charAt(0).toUpperCase() + species.diet.slice(1)} • ${species.category}`;
        hint.style.display = 'inline-block';
      }
    });
  }
  let lastResult = null;
  function bindCalculate() {
    $('#fc-btn-calculate').addEventListener('click', calculate);
  }
  function calculate() {
    const id = $('#fc-species').value;
    const stage = $('#fc-stage').value;        // adult | juvenile
    const count = parseInt($('#fc-count').value, 10) || 1;
    if (!id) {
      flashError('Please select a fish species first.');
      return;
    }
    const species = SPECIES.find(s => s.id === id);
    if (!species) return;
    const mode = stage === 'juvenile' ? species.juvenileMode : species.adultMode;
    lastResult = { species, stage, count, mode, timestamp: new Date() };
    renderResult(lastResult);
  }
  function renderResult(r) {
    const empty = $('#fc-result-empty');
    const filled = $('#fc-result-filled');
    if (empty) empty.style.display = 'none';
    if (filled) filled.style.display = 'block';
    $('#fc-out-species').textContent = r.species.name;
    $('#fc-out-tag').textContent = `${r.species.diet} • ${r.species.category} • ${r.stage}`;
    // Schedule
    $('#fc-out-times').innerHTML = `<strong>${r.mode.times}</strong> <span class="unit">×/day</span>`;
    $('#fc-out-perfeed').textContent = r.mode.perFeed;
    $('#fc-out-total').textContent = r.mode.total;
    // Schedule clock visualisation
    renderScheduleClock(r.mode.times);
    // Recommended foods
    const foodList = $('#fc-out-foods');
    foodList.innerHTML = '';
    r.species.foods.forEach((f, i) => {
      const li = document.createElement('li');
      li.innerHTML = `<span class="fc-food-rank">${i + 1}</span> ${f}`;
      foodList.appendChild(li);
    });
    // Avoid list
    const avoidWrap = $('#fc-out-avoid-wrap');
    if (r.species.avoid.length > 0) {
      const avoidList = $('#fc-out-avoid');
      avoidList.innerHTML = '';
      r.species.avoid.forEach(a => {
        const li = document.createElement('li');
        li.textContent = a;
        avoidList.appendChild(li);
      });
      avoidWrap.style.display = 'block';
    } else {
      avoidWrap.style.display = 'none';
    }
    // Tank-wide multiplier note
    if (r.count > 1) {
      $('#fc-out-multiplier').innerHTML = `For your group of <strong>${r.count}</strong>, multiply per-feed amounts above. Watch for leftover food after 60-90 seconds and reduce if needed.`;
      $('#fc-out-multiplier').style.display = 'block';
    } else {
      $('#fc-out-multiplier').style.display = 'none';
    }
    // Fasting note
    $('#fc-out-fasting').style.display = r.species.fastDay ? 'flex' : 'none';
    // Notes
    $('#fc-out-notes').textContent = r.species.notes;
  }
  function renderScheduleClock(times) {
    const wrap = $('#fc-clock');
    if (!wrap) return;
    wrap.innerHTML = '';
    // Generate suggested feeding times
    const schedules = {
      1: ['18:00'],
      2: ['08:00', '18:00'],
      3: ['08:00', '13:00', '18:00'],
      4: ['08:00', '12:00', '15:00', '19:00'],
      5: ['08:00', '11:00', '13:00', '16:00', '19:00']
    };
    const suggested = schedules[times] || schedules[2];
    suggested.forEach(t => {
      const pill = document.createElement('span');
      pill.className = 'fc-time-pill';
      pill.innerHTML = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg>${t}`;
      wrap.appendChild(pill);
    });
  }
  function flashError(msg) {
    const empty = $('#fc-result-empty');
    const filled = $('#fc-result-filled');
    if (empty) empty.style.display = 'grid';
    if (filled) filled.style.display = 'none';
    const note = $('#fc-result-error');
    if (note) {
      note.textContent = msg;
      note.style.display = 'block';
      setTimeout(() => { note.style.display = 'none'; }, 3500);
    }
  }
  function bindReset() {
    $('#fc-btn-reset').addEventListener('click', () => {
      $('#fc-species').selectedIndex = 0;
      $('#fc-stage').value = 'adult';
      $('#fc-count').value = '';
      $('#fc-species-hint').style.display = 'none';
      const empty = $('#fc-result-empty');
      const filled = $('#fc-result-filled');
      if (empty) empty.style.display = 'grid';
      if (filled) filled.style.display = 'none';
      lastResult = null;
    });
  }
  function bindExports() {
    $('#fc-btn-csv')?.addEventListener('click', exportCSV);
    $('#fc-btn-print')?.addEventListener('click', () => window.print());
    $('#fc-btn-copy')?.addEventListener('click', copyResult);
  }
  function exportCSV() {
    if (!lastResult) { flashError('Calculate first to export.'); return; }
    const r = lastResult;
    const rows = [
      ['Fishhear Feeding Plan'],
      ['Generated', r.timestamp.toISOString()],
      ['Species', r.species.name],
      ['Category', r.species.category],
      ['Diet', r.species.diet],
      ['Life stage', r.stage],
      ['Number of fish', r.count],
      [],
      ['Schedule'],
      ['Feedings per day', r.mode.times],
      ['Per-feed amount', r.mode.perFeed],
      ['Daily total guideline', r.mode.total],
      ['Weekly fast day recommended', r.species.fastDay ? 'Yes' : 'No'],
      [],
      ['Recommended foods (priority order)'],
      ...r.species.foods.map((f, i) => [`#${i + 1}`, f]),
    ];
    if (r.species.avoid.length > 0) {
      rows.push([], ['Foods to avoid']);
      r.species.avoid.forEach(a => rows.push([a]));
    }
    rows.push([], ['Species notes'], [r.species.notes]);
    const csv = rows.map(row => row.map(cell => {
      const s = String(cell ?? '');
      return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
    }).join(',')).join('\n');
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `fishhear-feeding-${r.species.id}-${Date.now()}.csv`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }
  async function copyResult() {
    if (!lastResult) { flashError('Calculate first to copy.'); return; }
    const r = lastResult;
    const I = window.FishhearI18N;
    const txt = `Fishhear Feeding Plan
Species: ${r.species.name} (${r.species.diet}, ${r.species.category})
Life stage: ${r.stage}
${r.count > 1 ? 'For ' + r.count + ' fish' : ''}
Schedule: ${r.mode.times}× per day, ${r.mode.perFeed} each feeding
Daily total: ${r.mode.total}
${r.species.fastDay ? 'Recommended weekly fast day: yes\n' : ''}
Recommended foods (priority order):
${r.species.foods.map((f, i) => `  ${i + 1}. ${f}`).join('\n')}
${r.species.avoid.length ? '\nAvoid:\n' + r.species.avoid.map(a => '  - ' + a).join('\n') : ''}
Notes: ${r.species.notes}${I ? '\n\n' + buildSpokenFeedingSentence(r) : ''}`;
    if (I) {
      await I.copyText(txt, $('#fc-btn-copy'));
      return;
    }
    try {
      await navigator.clipboard.writeText(txt);
      const btn = $('#fc-btn-copy');
      const original = btn.innerHTML;
      btn.innerHTML = '✓ Copied';
      setTimeout(() => { btn.innerHTML = original; }, 1800);
    } catch (e) {
      flashError('Could not copy. Browser blocked clipboard access.');
    }
  }
  /* ---------- Boot ---------- */
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();